# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env — 실제 값은 절대 커밋하지 않는다 (AGENTS "절대 하지 말 것")
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# testing
/coverage

# prisma
/prisma/generated

# vercel — 프로젝트 링크(projectId·orgId)와 CLI가 내려주는 토큰
.vercel

# docs — 설계 문서·ADR·리팩터 노트는 로컬에만 둔다
/docs

# 작업물 스크린샷의 굽기 전 원본 (ADR-005). webp만 올라간다 —
# 원본은 곁에 둬도 되지만 레포에 들어가면 그대로 서빙되고 굽는 의미가 없어진다
public/projects/*/origin/
