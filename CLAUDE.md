# CLAUDE.md

> **먼저 `AGENTS.md`를 읽어라.** 프로젝트 규칙·컨벤션·금지사항·문서 순서는 전부 거기에 있다(단일 진실).
> 이 문서는 그 규칙을 **중복하지 않고**, Claude Code 세션에서만 필요한 운영 팁을 얇게 얹는다.
> 규칙이 충돌하면 `AGENTS.md`와 `docs/decisions/`가 이긴다.

## 이 프로젝트에서 Claude Code의 기본 자세

- 이 레포는 **1인 전용 도구**다. "남도 쓸 수 있게" 일반화하거나 범용 추상화를 끼워 넣지 않는다(`docs/00` §5.1 영구 Non-goal).
- `docs/`가 진실의 원천이다. 구현이 문서와 어긋나면 **문서를 신뢰하고**, 문서를 바꿔야 하면 먼저 제안한다. ADR 대상 결정은 임의로 뒤집지 않는다.
- 작업 전 현재 마일스톤(`docs/07` §2)과 그 DoD를 확인하고, DoD를 넘기는 최소 범위로 자른다.

## 커밋 (커밋 컨벤션 스킬 연동)

`AGENTS.md`의 커밋 규칙을 그대로 따른다. 요약:

- Conventional Commits, **제목 영문 소문자 / 본문 한글**
- **Claude 어트리뷰션 푸터 금지**(`Co-Authored-By`, `Generated with` 등 일절)
- main 직접 작업 금지 → `feat/`·`fix/`·`chore/` feature 브랜치
- 한 커밋 = 한 논리 변경. 논리적으로 쪼갤 수 있으면 나눈다
- 여러 줄 본문은 HEREDOC로 작성

## 자주 쓰는 명령

```bash
npm run dev                  # 로컬 개발 (?site= 쿼리로 3면 전환)
npm run build                # 커밋 전 필수 통과
npm run test                 # Vitest 단위 테스트
npx prisma migrate dev       # 스키마 변경 (수동 SQL·마이그레이션 편집 금지)
npm run crawl -- --dry-run   # 크롤러 파싱 검증 (ingest POST 없이 6질문·4그룹 확인)
npm run migrate -- --dry-run # 티스토리 마이그레이션 리포트 (DB 미기록)
npm run release -- --dry-run # 프로덕션 배포 (검사만; 실제 배포는 플래그 없이)
```

> `main` 머지는 배포가 아니다(Hobby가 조직 private 레포 연동을 막는다). 배포는 `npm run release`로
> 손으로 돌린다 — 작업 트리가 더럽거나 `main`이 안 푼 커밋을 들고 있거나 CI가 실패한 커밋이면 멈춘다.
> `scripts/deploy.mts`는 이름이 비슷하지만 다른 것이다: Vercel **빌드 안에서** 마이그레이션을 돌린다.

## 세션 운영 시 주의 (이 프로젝트 특유의 함정)

- **비밀 값을 코드/문서/커밋에 절대 남기지 않는다.** 새 시크릿이 필요하면 env 키 이름만 코드에 두고, 값은 Vercel/Actions Secrets에 넣도록 안내한다.
- **크롤러·마이그레이션 작업**은 항상 dry-run + fixture 테스트를 먼저 통과시키고 커밋한다. "빈 것처럼 보임"을 SKIP 근거로 삼지 않는다(파싱 실패는 FAILED로 시끄럽게).
- **content를 만질 때**는 반드시 `lib/content`의 Zod 스키마를 경유한다. `Json`을 리포지토리 밖으로 흘리지 않는다.
- **공개 페이지에 클라이언트 코드**를 늘리지 않는다(아일랜드 **UI 6개 + 그리지 않는 것 2개** — 정본은 `docs/04` §3.6). 인터랙션이 아니면 서버 컴포넌트.
- **설교 에디터**를 건드릴 때는 "방해 요소 제로 · 로컬 우선 무손실"이 최우선 불변식이다(프리모템 #2). 팝업·자동 포커스 이동·발행 확인 모달을 넣지 않는다.
- 큰 변경은 계획을 먼저 제시하고 승인받는다. 요청 없는 대규모 리팩토링 금지.

## 완료 보고 형식

간결하게: **변경 파일 / 핵심 결정 / 남은 일** 세 줄. 장황한 설명 대신 diff와 근거를 남긴다.
